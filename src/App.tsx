// Placeholder shell for the core-functionality phase.
// The real UI (category picker, timer, summary, daily) is built in the UI phase (plan tasks 6–11).
export default function App() {
  return (
    <main className="app-shell">
      <h1>Timebox</h1>
      <p>核心功能开发中 · UI 即将到来</p>
    </main>
  )
}
